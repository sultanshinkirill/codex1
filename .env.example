# Free AutoFrame - Environment Variables Template
# Copy this file to .env and fill in your values

# ============================================
# DEPLOYMENT MODE
# ============================================
# Options: development, vercel, hostinger
DEPLOYMENT_MODE=development

# ============================================
# FLASK CONFIGURATION
# ============================================
FLASK_ENV=production
# CRITICAL: Must be set in production! Use: openssl rand -hex 32
SECRET_KEY=your-secret-key-here-change-this-in-production

# ============================================
# API ENDPOINTS
# ============================================
# For Vercel: Point to Hostinger backend for heavy processing
# For Hostinger: Point to self
BACKEND_API_URL=https://api.yourdomain.com
HOSTINGER_API_URL=https://api.yourdomain.com

# Frontend URL (for CORS)
FRONTEND_URL=https://autoframe.vercel.app

# ============================================
# VIDEO PROCESSING LIMITS
# ============================================
# Client-side duration limit (seconds) - videos longer than this go to server
CLIENT_DURATION_LIMIT_SECONDS=75

# Maximum video duration for server processing (seconds)
# PAID tier: 180s (3 minutes), FREE tier: 75s (enforced client-side)
MAX_SERVER_DURATION_SECONDS=180

# Maximum file size (MB) - should be 400MB to allow 300MB PAID tier limit
MAX_CONTENT_LENGTH_MB=400

# Maximum number of videos in a batch
# PAID tier: 20 videos, FREE tier: 3 videos
MAX_BATCH_SIZE=20

# ============================================
# TIER SYSTEM (FREE vs PAID)
# ============================================
# FREE tier limits:
# - 3 videos max (50MB each)
# - 75 seconds duration
# - 2 aspect ratios
# - 3 batches per day
# - Browser rendering only
#
# PAID tier limits:
# - 20 videos max (300MB each)
# - 180 seconds (3 minutes) duration
# - 4 aspect ratios
# - Unlimited batches
# - Server rendering (faster)

# ============================================
# PARALLEL PROCESSING (Hostinger only)
# ============================================
# Number of videos to process simultaneously
# Recommended: (CPU cores / 2)
# VPS 2 (2 vCPU): 1
# VPS 4 (4 vCPU): 2
# VPS 8 (8 vCPU): 4
MAX_PARALLEL_JOBS=2

# ============================================
# CORS CONFIGURATION
# ============================================
# Comma-separated list of allowed origins
CORS_ORIGINS=https://autoframe.vercel.app,https://yourdomain.com

# ============================================
# FILE CLEANUP
# ============================================
# Auto-delete processed files after X hours (default: 1 hour)
# Set to 24 for daily cleanup, 1 for hourly cleanup
AUTO_CLEANUP_HOURS=1

# ============================================
# RATE LIMITING
# ============================================
# Rate limits are enforced using composite key (session + IP)
# to prevent bypass via cookie clearing or incognito mode
#
# Default limits:
# - /api/process: 10 requests per hour (server rendering)
# - /increment-usage: 20 requests per hour (client rendering)
# - Global: 200 requests per hour (all other endpoints)

# ============================================
# DEBUGGING (Development only)
# ============================================
# Enable remote debugging
ENABLE_DEBUG=false
DEBUG_PORT=5678

# ============================================
# GOOGLE ADSENSE (Optional)
# ============================================
ADSENSE_CLIENT=ca-pub-your-adsense-id
ADSENSE_SLOT_REWARDED=your-rewarded-ad-slot-id
ADSENSE_SLOT_DISPLAY=your-display-ad-slot-id
